<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">映画の編集</h1>

  <%= render "form", movie: @movie %>

  <%= link_to "見せる", @movie, class: "btn btn-secondary" %>
  <%= link_to "映画に戻る", movies_path, class: "btn btn-ghost" %>
</div>
