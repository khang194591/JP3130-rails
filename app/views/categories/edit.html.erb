<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">カテゴリの編集</h1>

  <%= render "form", category: @category %>

  <%= link_to "見せる", @category, class: "btn btn-secondary" %>
  <%= link_to "カテゴリーに戻る", categories_path, class: "btn btn-ghost" %>
</div>
